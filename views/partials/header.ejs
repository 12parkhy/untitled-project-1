<div style="margin-bottom: 7rem;">
    <nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-primary">
        <a class="navbar-brand" href="/users">J Library</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link" href="/users">Home <span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownInfo" style="color: white" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Hello, <%= user.name %></a>
                    <div class="dropdown-menu" id="dropdown-info-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="/users/<%= user.id %>">My Profile</a>
                        <div class="dropdown-divider"></div>
                        <form action="/users/logout?_method=DELETE" method="POST">
                            <button type="submit" class="btn btn-primary nav-link" id="logout-button">Log Out</button>
                        </form>
                    </div>
                </li>
                <% if (user.email != admin) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/users/searchbooks" style="color: white;">Search Books</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/users/status" style="color: white;">Status</a>
                    </li>
                    <% } %>
                        <% if (user.email == admin) { %>
                            <li class="nav-item">
                                <a class="nav-link" href="/manageusers" style="color: white;">Manage Users</a>
                            </li>
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownBooks" style="color: white;" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                              Manage Books</a>
                                <div class="dropdown-menu" id="dropdown-books-menu" aria-labelledby="navbarDropdown">
                                    <a class="dropdown-item" href="/books/add">Add Books</a>
                                    <div class="dropdown-divider"></div>
                                    <a class="dropdown-item" href="/books">Edit/Delete Books</a>
                                </div>
                            </li>
                            <% } %>
            </ul>
        </div>
    </nav>
</div>